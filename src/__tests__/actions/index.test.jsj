import * as actions from './../../actions';

describe('hangman actions', () => {
  it('setGoalWord should create SET_GOALWORD action', () =>{
    expection(actions.setGoalWord()).toEqual({
      type: 'SET_GOALWORD'
    });
  });
});